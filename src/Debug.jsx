import { useState, useEffect, useRef } from "react"
import LocalData from "./LocalData.json"

const Debug = () => {
    // console.log(LocalData)
    // console.log(Object.entries(LocalData).slice(0, 5))

    return (
        <>

        </>
    );
}

export default Debug;